# --- Database ---
DATABASE_URL=postgresql+asyncpg://cortex:cortex_dev@localhost:5432/cortex_dev
REDIS_URL=redis://localhost:6379/0

# --- Clerk (authentication) ---
# Values come from the Clerk dashboard (Developers -> API Keys) for the
# practice/instance. Replace <instance> with the actual instance slug,
# e.g. https://your-app.clerk.accounts.dev
CLERK_SECRET_KEY=sk_test_replace_me
CLERK_JWKS_URL=https://<instance>.clerk.accounts.dev/.well-known/jwks.json
CLERK_ISSUER=https://<instance>.clerk.accounts.dev
# Only needed later for the frontend UI (Task 0.4) — documented here now.
CLERK_PUBLISHABLE_KEY=pk_test_replace_me
